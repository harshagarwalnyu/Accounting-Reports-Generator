# Copy this file to .env and fill in your values.
# Port overrides — if unset, the startup scripts pick free ports automatically.
FRONTEND_PORT=3000
BACKEND_PORT=8000

# Optional — enables AI features (column detection, account classification, narratives)
ANTHROPIC_API_KEY=your_key_here

